<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- BrandBadge as a favicon: orange rounded square, white "E." (DESIGN.md —
       white-on-orange is the logo mark's alone). Fixed colors on purpose: the
       icon is self-contained and reads on both light and dark themes. -->
  <rect width="64" height="64" rx="8" fill="#f5a100"/>
  <text x="30" y="46" font-family="Arial, Helvetica, sans-serif" font-size="42" font-weight="700" fill="#ffffff" text-anchor="middle">E.</text>
</svg>
