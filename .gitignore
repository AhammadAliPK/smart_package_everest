.claude/settings.local.json
.DS_Store

# dependencies
node_modules/

# build output
dist/
.turbo/

# generated Prisma client (regenerated by `prisma generate` on install/build)
apps/api/src/adapters/db/generated/

# test coverage
coverage/

# env files (commit .env.example, never .env)
.env
.env.local
.env.*.local

# turbo prune output (docker build)
out/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# vitest HTML/UI reports (throwaway output of --reporter=html / --ui)
.vitest/
# feature-grouped test report (pnpm run test:report)
test-report/
