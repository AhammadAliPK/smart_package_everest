---
scope: Smart Package Locker Management System — REST API, greenfield, from docs/prd.md
purpose: build-substrate feeding bmad-create-epics-and-stories and bmad-build
altitude: feature (keeps epics)
updated: 2026-09-13T14:29
---

- (decision) Interface: REST API [ADOPTED] — user chose over CLI/UI (PRD left it open); binds all levels, prevents per-level interface drift
- (constraint) PRD docs/prd.md is the authoritative requirements source: Levels 1-4 (storage, retrieval, tiered charges X/2X/3X per 5-day bands, 24h=1 day, optional concurrency); smallest-fitting-locker assignment; one package per locker; unique pickup codes; extensible design required
- (decision) Spine is build-substrate only (no extra human artifact); downstream = bmad-create-epics-and-stories then bmad-build; working mode = Coaching path [ADOPTED]
- (decision) Stack: TypeScript + Fastify [ADOPTED by user]; prevents per-module framework drift; validation + OpenAPI approach to be fixed as AD
- (decision) Persistence: PostgreSQL [ADOPTED by user, overrode in-memory lean]; Level-4 concurrency to be solved with DB transactions + FOR UPDATE SKIP LOCKED, not in-process locks
- (decision) Scope: Level 4 (concurrent storage requests) included as its own epic [ADOPTED by user]
- (decision) Data access: Prisma [ADOPTED by user]; migration story via prisma migrate; Level-4 allocation as FOR UPDATE SKIP LOCKED inside
- (decision) Deployment: Docker [ADOPTED by user — target Railway]; Dockerfile for Railway deploy, docker compose for local dev/test (postgres + api); prevents local-prod env drift
- (decision) Validation & docs: TypeBox schemas via @fastify/type-provider-type-box + @fastify/swagger for OpenAPI /docs [ASSUMPTION pending review]
- (decision) Tests: Vitest against dedicated test database on the same compose Postgres (truncate between tests) [ASSUMPTION pending review]
- (assumption) Day counting for Level 3: days = ceil(elapsed_hours / 24) — partial day counts as a full day; tiers: days 1-5 X/day, 6-10 2X/day, 11+ 3X/day; X configurable via env STORAGE_FEE_BASE (default 10 units) [needs user confirm]
- (decision) Paradigm: hexagonal (ports & adapters) — domain (allocation rule, tiered pricing, code validation) pure TS, application use-cases, adapters: fastify routes + prisma repos; satisfies PRD extensibility rule
- (version) Stack verified 2026-09-13 (npm registry + postgresql.org): typescript 7.0.2, fastify 5.12.4, @fastify/swagger 9.8.1, @sinclair/typebox 0.34.52, prisma 7.10.0 (latest tag is 8.0.0-rc.14 — RC, not pinned), vitest 5.0.0, postgresql 18 (image postgres:18-alpine)
- (decision) Monorepo: Turborepo 2.10.12 + pnpm 12.4.1 workspaces [ADOPTED by user mid-review]; layout apps/api + packages/domain; domain as its own workspace package structurally enforces AD-2 (cannot import fastify/prisma — not in its package.json); docker build via turbo prune; Railway deploys monorepo root / apps/api
- (version) turborepo 2.10.12, pnpm 12.4.1 (npm registry, verified 2026-09-13)
- (decision) Gate fix (version reviewer): typebox 1.3.30 + @fastify/type-provider-type-box 6.1.0 replaces legacy @sinclair/typebox 0.34.52 (provider now peers on the new typebox pkg); @fastify/swagger-ui 6.1.1 added (swagger alone is JSON-only, AD-1 promises /docs); Node bumped 22->24 LTS (22 is maintenance-only since 2025-10; Prisma 7 accepts >=24); AD-3/stack notes Prisma v7 API is prisma.$transaction + tx.$queryRaw (v8 docs show db.transaction — don't follow them)
- (decision) Gate fixes (rubric walker): LOCKER_EMPTY 409 added to AD-7 (PRD invalid-scenario coverage); AD-8 now pins prisma migrate deploy in container entrypoint + Railway-provisioned Postgres/DATABASE_URL; AD-2 enforcement mechanical via ESLint no-restricted-imports in turbo lint; AD-3 pins $queryRaw for FOR UPDATE SKIP LOCKED (fluent API cannot express row locks); ERD pickupCode uniqueness annotated partial (active packages only), matching AD-6
- (decision) Gate fixes (adversarial): new AD-9 single locker identifier (cuid lockerId everywhere, no display code); AD-6 retrieval resolves in one query pickupCode+lockerId+status=STORED, codes never regenerated; AD-4 every occupancy mutation is CAS on occupiedBy or FOR UPDATE, never blind boolean; AD-1 freezes GET /lockers shape verbatim {lockers:[{id,size,occupied}]} ordered by id; AD-3 pins READ COMMITTED only, P2034 -> 3x bounded retry -> INTERNAL_ERROR; money pinned as plain integer units (rejected reviewer minor-units variant: PRD says 'X units/day', translation layer adds ambiguity); ERD occupied -> occupiedBy nullable CAS target
- (decision) Day counting = ceil (started day counts as full day) [ADOPTED by user 2026-09-13]; last open assumption resolved
- (event) spine finalized — gate passed (3 reviewers, 15 findings applied: version pins, LOCKER_EMPTY, migrate-deploy entrypoint, mechanical AD-2, raw-SQL row locks, AD-9 identity, CAS occupancy, frozen list contract, READ COMMITTED, plain integer units, ceil days); no open questions remain
- (decision) Scope update [ADOPTED by user]: full system, not REST-only — apps/web (Vite+React 19.3.0 SPA, Tailwind 4.3.3) + packages/ui (@locker/ui shadcn 4.21.0 component library, presentation-only). New AD-10: API sole state authority, web computes no domain outcomes, OpenAPI-generated client types; UI theme to derive from the source PDF's visual identity (user directive)
