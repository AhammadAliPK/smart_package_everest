# Build Handoff — read this first in any new session

Single source of truth for where the Everest build stands. Updated 2026-09-14 ~20:30 — **PROJECT COMPLETE: Epics 1–5 done.** Live at everest-api-sxla / everest-web-mi84 (Render, free tier). README shipped with disclosure; epic tags handed to the user. What remains is `deferred-work.md` only.

## Process rules (user-set, non-negotiable)

1. **User commits everything.** Agents never `git commit`/`add`/`stash`. Every dispatch ends with a COMMIT PLAN: per-story, test-first order, conventional message + exact paths. Hand it to the user verbatim.
2. **Speed over ceremony.** One dispatch per epic-batch. Full `turbo run build test lint` only at story boundaries. No multi-layer review agents during feature build — orchestrator spot-checks the risky files; everything else goes to `deferred-work.md` (post-deploy hardening pass).
3. **Machine:** pnpm = `npx pnpm@12.4.1` (corepack broken). Compose Postgres 18 already running (`locker-postgres`, host port **55432**; `.env` points DATABASE_URL there). Don't restart Docker. Local Node 22 is fine.
4. One command at a time; no parallel agents; no dev servers/watchers (tests use `app.inject()`).

## Where we are

- **Done:** Epics 1–4 COMPLETE. Epic 1–3 committed through `4849d22` (API: scaffold, lockers CRUD surface, POST /packages SKIP LOCKED smallest-fit + pickup codes + 409 refusal, POST /pickups atomic retrieval + four calm outcomes + AD-5 tiered charges, concurrency proofs). Epic 4 (`spec-4-1-4-7-web-ui.md`, status done) — the web UI: React 19.3 + Vite 8 + Tailwind 4 SPA in `apps/web`, `@locker/ui` presentation-only token/design system (dark default + persisted light toggle, pre-paint class), chooser landing, agent console (LockerGrid 10s poll pausing on hidden tab, stale label, create-locker dialog, empty station), store flow (free-tile prefill → ResultCard climax: oversized code, one-tap copy, focus+announce), `/retrieve` phone-first (CodeInput 8-cell with auto-advance/paste/backspace + banned-char hint, error-preserve, ChargeSummary ledger verbatim), 4.7 hardening (state-matrix audit, WCAG contrast computed from shipped tokens, banned-list scan). All types generated from the API's real OpenAPI (`schema.d.ts`, AD-10). Commits: `36bdb3c` 4.1 · `7c82abd` 4.2 · `a98cb70` 4.3 · `d8d00fc` 4.4 · `456d9d0` 4.5 · `6a7e950` 4.6 · 4.7 + this docs refresh at the top of the log. Tests 218 (domain 37, api 103, web 78); full pipeline ×3 consecutive green.
- **Done (cont.): AD-9 amendment v1.1 — human locker ids** (`spec-ad9-amendment-human-locker-ids.md`): the 25-char cuid is GONE. `locker.id` is now a generated 6-char value over the unambiguous pickup-code alphabet (`K7Q4M2` — no 0/O/1/I), and it IS the primary key — no surrogate key, no second column (user renegotiated the drafted "code column" mid-build: one table, one id). Create retries the ~1/887M collision (bounded); retrieval trims + uppercases; the `/retrieve` input auto-uppercases with `placeholder="e.g. K7Q4M2"`. Migration `20260913231500_human_locker_ids` swaps the PK in place (md5-derived backfill; applied to dev, nothing in production). Tests 227 (domain 41, api 105, web 81). **User must restart both dev servers before re-smoking** (domain dist + prisma client regenerated).
- **Deviations recorded in the Epic 4 spec's Implementation Notes:** `apps/web` alone pins typescript 5.9.3 (TS7 native has no JS compiler API; openapi-typescript needs it); light-theme ring darkened to `#9C6400` (brand `#F5A100` ring is ~1.9:1 on light paper, under the 3:1 WCAG 2.4.11 floor); CodeInput deliberately duplicates the 0/O/1/I display rule (web is banned from `@locker/domain`).
- **Pending user action:** manual smoke of the running stack (spec verification item, unticked until they pass): postgres + `npx pnpm@12.4.1 --filter @locker/api dev` + `npx pnpm@12.4.1 --filter @locker/web dev`, exercise chooser → store → retrieve → errors → themes.
- **Epic 5 / 5.1 code DONE (spec-5-1-5-2, T1–T5 + T7 partial):** slim prod-deps API runner (~705 MB layer; `prisma`+`dotenv` → deps; two cold-build fixes — tsconfig staged before install, domain→api via pnpm not turbo), NEW web Dockerfile (nginx:1.27-alpine, `try_files` deep links, `${PORT}` envsubst), NEW optional `CORS_ORIGIN` on the API (`@fastify/cors` 11.3.0 registered only when set; 8 new tests), compose full stack (web service, `restart: unless-stopped`, `start_period`, **credential-free per user amendment**: `:?` guards + assembled URL; run = `cp .env.example .env && docker compose up --build`), `render.yaml` blueprint (3 free resources, fromDatabase injection, `sync:false` on both cross-referencing literals). Working-tree compose validated end-to-end (health, deep link, store→retrieve, gzip; isolated ports; dev postgres untouched). **236 tests** (domain 41, api 113, web 82), 16/16 pipeline green twice.
- **Manual-smoke fixes (Epic-4 follow-ups, same session):** ResultCard values `whitespace-nowrap` (code folded at the hyphen) + guard test; CodeInput restructured into real `XXXX`/`XXXX` chunks with FLUID cells (`flex-1`, 44px floor) so the control always spans the section width — never overflows (user finding: 8th cell stuck out past the locker input), stacks 4+4 below 480px, dash joins wide; `/retrieve` section widened 520 → 600px (user request); last cuid-shaped fixture gone (state-matrix). HMR live — user re-smokes on the running dev stack.
- **Clean-checkout compose proof PASSED (2026-09-14):** clone of `840413d` to /tmp, `cp .env.example .env`, container names overridden (proof-*), isolated ports (pg 55433 / api 3011 / web 8089, CORS + baked VITE_API_BASE_URL matched). Verified: 3/3 containers healthy, `GET /health` ok, SPA deep link `/retrieve` 200 via nginx try_files, gzip on JS assets, Swagger UI at `/docs`, raw spec at `/docs/json`, full round trip `POST /lockers` → `POST /packages` → `POST /pickups` (6-char id `ENDTPE`, 8-char code, tier-1 charge ledger verbatim) and calm `LOCKER_EMPTY` 409 on code reuse. Stack torn down (`down -v`), clone removed, dev `locker-postgres` untouched.
- **CodeInput overflow root cause (fixed in `840413d`):** chunks had flex `min-width:auto` → input intrinsic (~170px) clamped by the 56px cell cap floored each chunk at 248px → 533px row that overflowed every column (61px at desktop). Fix: `min-w-0` on chunks + container query (`@container`, one row at ≥28rem). Verified in headless Chrome at 500–1280px: cell 8 right edge == button right edge exactly; 236 tests green.
- **Test visualization (user decision: local reports, no app changes):** `@vitest/ui@5.0.0` added at workspace root (also un-gates the built-in html reporter, which is `@vitest/ui/reporter`). `npx pnpm@12.4.1 --filter <pkg> exec vitest run --reporter=html` writes `<pkg>/.vitest/index.html` (gitignored); Karma-style live dashboard: `npx pnpm@12.4.1 --filter @locker/web exec vitest --ui` (watcher — run on demand only).
- **T6 LIVE DEPLOY DONE (2026-09-14):** blueprint applied from the user's Render (GitHub) account. Slug collisions suffixed both services — live URLs: API `https://everest-api-sxla.onrender.com`, web `https://everest-web-mi84.onrender.com`; both dashboard env literals updated + mirrored into render.yaml. Production smoke (all green): web bundle bakes the suffixed API URL, CORS allows the real web origin, `/retrieve` deep link 200, store→retrieve round trip (`RPH58K` / tier-1 ledger verbatim), calm `LOCKER_EMPTY` 409, `/docs` 200, gzip on assets. Free-tier: services sleep after ~15 min idle (30–60s wake); Postgres free-trial clock started at apply (expires ~10-14, after the deadline). Blueprint gotcha hit + fixed: envVars cannot carry both `value` and `sync:false`.
- **Next: (1)** Story 5.2 README (+ test matrix, `pnpm run test:report` how-to, API docs pointers `/docs` + `/docs/json`, live URLs, AI-use disclosure) + `epic-1..5` git tags → Epic 5 done. Deadline ~2026-09-18.
- **Environment now:** `locker-postgres` container already running on host port 55432 (`.env` set); Node 22 + `npx pnpm@12.4.1`; nothing else running.

## Workflow skeleton (bmad-build, compressed)

Per batch: write combined spec from `epics.md` story ACs + spine ADs → one AskUserQuestion approval (freeze) → sync `sprint-status.yaml` (stories → in-progress) → dispatch ONE general-purpose agent with: spec as sole source of truth, machine realities above, the no-commit rule + commit-plan requirement, story-boundary-only full pipeline runs → on return: spot-check risky code (allocation transaction, code gen, error mapping), re-run pipelines once, tick spec tasks + append Implementation Notes + status done, sync sprint-status (done), hand commit plan to user. Specs live in this directory; sprint status in `sprint-status.yaml`; deferred hardening in `deferred-work.md`.

## Key contract reminders (from ARCHITECTURE-SPINE.md, binding)

- GET /lockers items use **`id`** (frozen list contract `{"lockers":[{"id","size","occupied"}]}`, ordered by id); POST responses use **`lockerId`** (AD-9). **AD-9 v1.1:** every locker id is a generated 6-char unambiguous value (`K7Q4M2`); it is the `locker.id` PK — no cuids anywhere.
- Error envelope AD-7: NO_SUITABLE_LOCKER 409, INVALID_PICKUP_CODE 404, LOCKER_NOT_FOUND 404, LOCKER_EMPTY 409, VALIDATION_ERROR 400, INTERNAL_ERROR 500. One mapper exists (`apps/api/src/adapters/http/error-mapper.ts`), `AppError` carries code+status.
- Pricing (Epic 2): pure domain policy, integer units, days = ceil(elapsed/24h), 1–5×X / 6–10×2X / 11+×3X, X = STORAGE_FEE_BASE (default 10).
- Boundary lint (AD-2) enforced: domain + application zones forbid fastify/typebox/prisma imports; application also forbids adapters. Committed violating fixtures prove it (`scripts/check-boundaries.mjs`).
- Stack pins corrected: @sinclair/typebox **0.34.52**, @fastify/type-provider-typebox 6.1.0, + @prisma/adapter-pg 7.10.0 (Prisma 7 requires a driver adapter).
