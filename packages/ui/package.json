{
  "name": "@locker/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Presentational React component library (shadcn/Tailwind) — Epic 4 owns its internals. Placeholder until then (AD-10).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint ."
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
