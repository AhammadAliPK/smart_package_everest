{
  "name": "@locker/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain model for the locker system — entities, allocation rule, pricing policy, pickup codes. Zero runtime dependencies (AD-2).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
